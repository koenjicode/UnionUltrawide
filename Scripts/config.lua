

local _union_ultrawide_config = {

    -- Aspect Ratio Option
    -- 0 = Y-Axis FOV, 1 = X-Axis FOV, 2 = Major Axis
    axis_constraint_type = 1,

    -- Disables the aspect ratio lock on Menu screens.
    -- I would not recommend turning this feature on as a lot of menus are not designed for this.
    disable_aspect_ratio_in_all_scenes = true,
}

return _union_ultrawide_config